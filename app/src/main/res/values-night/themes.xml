<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PruebaMeli" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/brand_color</item>
        <item name="colorPrimaryDark">@color/brand_color</item>
        <item name="colorAccent">@color/brand_color</item>
    </style>
    <style name="Theme.PruebaMeli" parent="Base.Theme.PruebaMeli" />
</resources>